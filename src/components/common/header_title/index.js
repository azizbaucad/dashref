import { Box } from '@chakra-ui/react';
import { PageTitle } from '../title/page';

export const CustomHeaderTitle = ({ icon, title, subtitle }) => {
  return (
    <Box>
      <PageTitle />
    </Box>
  );
};
